<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ticket de Compra</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f4f4f4;
        }
        .ticket {
            width: 400px;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            text-align: center;
            color: #8a2626;
        }
        .details, .summary {
            margin-top: 20px;
        }
        .details p, .summary p {
            margin: 0;
            padding: 5px 0;
        }
        .summary {
            font-weight: bold;
            border-top: 1px solid #ddd;
            padding-top: 10px;
        }
        table {
            width: 100%;
            margin-top: 20px;
            border-collapse: collapse;
        }
        th, td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        th {
            background-color: #f4f4f4;
        }
        .total {
            text-align: right;
            font-size: 18px;
            color: #8a2626;
        }
    </style>
</head>
<body>
    <div class="ticket">
        <h1>¡Gracias por tu compra!</h1>

        <div class="details">
            <p><strong>Fecha y hora:</strong> {{ticketData.purchase_datetime}}</p>
            <p><strong>Comprado por:</strong> {{ticketData.purchaser}}</p>
        </div>

        <div class="summary">
            <h3>Detalles de la compra:</h3>
            <table>
                <thead>
                    <tr>
                        <th>Producto</th>
                        <th>Cantidad</th>
                        <th>Total</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each ticketData.products}}
                    <tr>
                        <td>{{this.title}}</td>
                        <td>{{this.quantity}}</td>
                        <td>${{this.subtotal}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>

            <p class="total">Total de la compra: ${{ticketData.amount}}</p>
        </div>

        <p style="text-align: center;">¡Esperamos verte pronto!</p>
    </div>
</body>
</html>